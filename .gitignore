node_modules
.next
out
.env*
!.env.example
.DS_Store
*.tsbuildinfo
next-env.d.ts
.vercel

# Brand deliverables (18MB incl. an 8.7MB zip). These are hand-off assets,
# not site code — keeping them out of git history.
brand/

# build artefact from scripts/build-staging.sh — never committed
staging-bundle.tgz
